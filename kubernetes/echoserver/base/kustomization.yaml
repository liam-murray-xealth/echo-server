apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app: echoserver
resources:
  - service-account.yaml
  - deployment.yaml
  - service.yaml

# For future (config not currently used)
configMapGenerator:
  - name: echoserver-config
    files:
      - ./files/config.yaml
generatorOptions:
  labels:
    type: generated


